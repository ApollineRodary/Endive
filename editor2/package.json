{
  "name": "endive-editor",
  "version": "0.1.0",
  "description": "A web-based editor for Endive",
  "scripts": {
    "build": "parcel build src/index.html",
    "serve": "parcel serve src/index.html",
    "format": "prettier --write \"src/**/*.{css,html,ts}\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "endive-wasm": "file:../crates/endive-wasm/pkg"
  }
}
